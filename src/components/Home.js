import React from 'react';
import { HomeScreen } from './HomeScreen'; // Adjust the path if necessary

function Home() {
  return (
    <div>
      <HomeScreen /> {/* Use the HomeScreen component here */}
    </div>
  );
}

export default Home;
